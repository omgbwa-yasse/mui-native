import type { TransitionBaseProps } from '../Fade/types';

export type { TransitionBaseProps } from '../Fade/types';

/** Zoom animates scale 0 ↔ 1. */
export interface ZoomProps extends TransitionBaseProps {}
