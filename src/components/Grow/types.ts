import type { TransitionBaseProps } from '../Fade/types';

export type { TransitionBaseProps, TransitionTimeout, TransitionTimeoutProp } from '../Fade/types';

/** Grow animates opacity 0→1 and scale 0.75→1. */
export interface GrowProps extends TransitionBaseProps {}
